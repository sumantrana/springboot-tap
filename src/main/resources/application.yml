client: "VMware"

framework: "Spring Boot with Maven"

spring:
  application:
    name: springboot-maven

logging:
  file:
    name: "/tmp/springboot-maven.log"
  level:
    com: DEBUG

configfrom: "YAML config (application.yml)"

msg_body: "With A Secure Software Supply Chain, Deploying Python Code To PROD is Safe, Reliable, And Fast!"
